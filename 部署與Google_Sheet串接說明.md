# 2026 北海道行動手冊｜GitHub Pages 部署說明

## 建議部署方式

1. 將本資料夾中的 `index.html` 上傳到 GitHub Repository 根目錄。
2. 到 Repository 的 **Settings → Pages**。
3. Source 選擇 **Deploy from a branch**，指定 `main` 與 `/root`。
4. 等候 GitHub Pages 產生網址。

`index.html` 已預設串接以下 GAS Web App：

`https://script.google.com/macros/s/AKfycbzf2wSoS5P0c12Hgq8S8z-8h3tpr15ZLpNCDexUGHVaewCNGFA3CgOFHlzIKUGF2A1S/exec`

## 本次版面與操作調整

- 日期選單改為手機三欄、寬螢幕六欄，不會再超出可點擊範圍。
- 官式拜會、參訪與餐飲使用不同卡片樣式；含青年提問或簡報者另有標記。
- 官式拜會的「行程細流」會併入同一張卡片完整顯示。
- Word 備註欄的文字顏色與螢光底色已帶入行程卡片。
- 旅程日期內會自動定位至目前時間點，亦可按「目前時段」重新定位。
- 移除首頁；底部導覽改為今日、行程、資料、搜尋。
- 設定頁新增返回按鈕，瀏覽器上一頁也可使用。
- 雲端同步改為背景局部更新，不會重建整個頁面、收合已展開內容或清除輸入草稿。
- 註解儲存改為一次 POST 直接進行版本衝突檢查，減少一次網路往返。

## 加速 GAS（建議）

網頁端已可直接使用目前 GAS 網址。若希望進一步縮短延遲，請將 `Google_Apps_Script_備註串接_Code.gs` 更新到原本 Apps Script 專案，然後：

1. 執行一次 `setupSheets()`。
2. 選擇 **部署 → 管理部署作業**。
3. 編輯原部署，版本選擇 **建立新版本**。
4. 執行身分選擇自己；存取權限依團隊需求設定。
5. 部署後 `/exec` 網址通常維持不變。

快速版不會在每次讀寫時重複設定工作表格式，並縮減試算表讀取範圍。

## 同步衝突

每筆註解均保有版本號。儲存時若 Google Sheet 已有較新版本，網頁會提供：下載最新版、載入最新版編輯、附加在最新版後、覆蓋最新版。

## 隱私提醒

HTML 內含聯絡電話、飲食、住宿、車次及工作分工等內部資訊。GitHub Pages 原則上可由網址公開存取，部署前請確認 Repository 與頁面公開範圍符合使用需求。

## v3 更新：原始備註圖片

行程卡片的「完整內容與工作手冊備註」已在原始 Word 對應位置呈現機場櫃台、Group Tag、集合位置圖，以及北海道廳與十勝綜合振興局的會場／座位配置圖。圖片已內嵌於 HTML，不需另行上傳圖片檔；手機可點選圖片放大查看。

## v4 更新：APP 化介面與每日總覽

- 行程頁改為白、黃、藍的 APP 式卡片介面，取消桌面版右側原始手冊區塊。
- 每一天最上方新增「今日路線、當日禮品、住宿／返程」摘要。
- 「今日」頁改為目前行程與下一步的儀表板，與「行程」完整時間軸區隔。
- 旅程當日首次開啟會直接進入當日完整時間軸，並定位至目前時段。
- 原始 Word 備註中的 7 張圖片仍依原位置顯示；含圖片的行程會預設展開並標示「原始附圖」。

## v5 更新：操作列、精準地圖與六日總覽

- 行程卡片的「即時註解／地圖／複製」操作列移到完整備註上方，展開長內容後仍可先操作。
- 完整備註只保留一個展開／收合提示。
- Google 地圖僅使用工作手冊中辨識出的地址；同一卡片有多個地點時先顯示地址選單。
- 今日路線補入午餐、晚餐及住宿資訊。
- 青年簡報與提問者會在卡片收合狀態下直接顯示。
- 新增「總覽」頁，以原手冊的黃色、青色、紫色及灰色邏輯呈現六日簡易總流程。
- Day 2 的 OSTERIA SALVATORE 行程改為午餐分類。

## v6 更新：每日 My Maps、點位工作分工與工作資料庫

- 每日行程頂部新增 Google 我的地圖入口，依 Day1–Day6 圖層名稱開啟共用路線圖。
- 今日路線中的官式拜會改為紅色醒目區塊，午晚餐與參訪亦有不同底色。
- 有明確工作分工的重點行程新增「工作分工」按鈕。
- 工作資料庫不再摺疊；前四張核心表格直接使用原始 Word 色塊與表格版面。
- 其他資料表改為完整展開、可水平滑動的 Word 式表格。

## v7 更新：青年通知總表與地圖按鈕範圍

- 每日行程總表新增「青年簡報／提問提醒」，列出場次、角色、姓名及 A／B／C 組。
- 同步顯示通知窗口：A 組慈璿、B 組廷娟、C 組昂臻。
- 「開啟地圖」只會出現在參訪與用餐行程；官式拜會、移動、住宿、集合與航班不再顯示。

## v8 更新：操作工具、預設頁面、精確晚餐地圖與原始表格

- 移除「複製資訊」及底部「今日」頁籤；開啟後預設進入行程頁。
- 旅程期間預設顯示當日，非旅程日期預設顯示 Day 1。
- 行程工具改為較小但保有手機觸控高度的文字按鈕。
- 展開「完整內容與工作手冊備註」時，標題列改為藍色狀態。
- 今日路線不再重複列出住宿；住宿仍保留於下方專區。
- Day 1 至 Day 5 晚餐使用指定的 Google Maps 精確短網址。
- 工作資料庫全面改用 Word 原始表格 HTML，保留合併儲存格、原始色塊及車次團隊配色。

## v9 更新：青年簡報／提問提醒判定修正

- 青年簡報與提問提醒改為只依工作手冊中已確認的場次清單顯示，不再以「青年團」「青年代表」等一般文字自動判定。
- Day 2「KOKO HOTEL 札幌薄野」「KOKO－索拉利亞西鐵」不顯示青年簡報／提問提醒。
- Day 5「08:30 集合、飯店 Check Out」不顯示青年簡報／提問提醒。


## v10 更新

- 頁首品牌列改為一般內容，不再跟隨捲動。
- 展開長篇工作手冊備註時，時間與行程標題會固定在日期列下方。
- 入住飯店行程補上指定 Google Maps 連結。
- 地圖改用目前頁面導向，不再開啟 about:blank 中介頁面。
